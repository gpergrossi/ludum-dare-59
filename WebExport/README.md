To test a web export:

* Export the project: `Project > Export ... > Web (Runnable) > Export Project ... > Save`

* In this dir, run `python server.py` (needs to be Python 3).

* open http://localhost:8000/Artifacts/web_export.html
